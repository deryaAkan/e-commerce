const initialState = {
  user: {
    name: "",
    email: "",
    password: "",
    role_id: "",
  },
};

const userReducer = (state = initialState, action) => {
  switch (action.type) {
    //ACTION CASELERİ
    default:
      return state;
  }
};
