const globalReducer = (
  state = { roles: [], categories: [], theme: "", language: "" },
  action
) => {
  switch (action.type) {
    //ACTION CASELERİ GELECEK

    default:
      return state;
  }
};
